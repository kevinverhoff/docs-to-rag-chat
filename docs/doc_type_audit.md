# Doc Type Audit

This file documents your organization's `doc_type` taxonomy decisions.

Use it to record:
- Working definitions for each doc type
- Logic for how documents are classified
- Manual overrides (add to `pipeline/build_overrides.py`)
- Audit decisions made during document review

See `docs/doc_types.md` for the current taxonomy reference.